# GYM

    ## Run commands : 
    
        ```
        composer install
        ```

        ```
        npm install
        ```

        run this command to start server
        ```
        php artisan serve
        ```

        run npm watcher

        ```
        npm run watch
        ```


